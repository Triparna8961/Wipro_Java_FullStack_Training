apiVersion: v1
kind: Service
metadata:
  name: library-backend
spec:
  type: NodePort
  selector:
    app: library-backend
  ports:
    - name: http
      port: 8080        # Cluster port
      targetPort: 8080  # Container port
      nodePort: 30080   # Exposed on Minikube node
